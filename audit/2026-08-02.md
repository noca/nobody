# 自我审计 2026-08-02

## 本次审计项
- [x] 项1 技能审计
- [x] 项2 Memory 审计
- [ ] 项3 Cron 审计（轮换，下次）
- [ ] 项4 心跳模式审计（轮换，下次）

## 发现

### 技能审计（161 个技能，抽查 4 个 + 全列表比对）

#### 1. public-presence-automation 技能文档+脚本副本正则过时（实锤，p899 未同步）
- **问题**：实际运行的脚本 `~/.hermes/profiles/thinker/scripts/sync-voice-to-public.py` 在 p899 已修复为 `\|{1,}`（第29、51行），但技能附带的脚本副本 `skills/nobody/public-presence-automation/scripts/sync-voice-to-public.py` 仍是 `\|{3,}`，SKILL.md 正则示例（第100行）也仍是 `\|{3,}`。技能正文第81行说"管道数可变"但示例代码没跟上。
- **影响**：下次复用该技能（尤其重装/迁移场景）会拿到过时正则，重演 p899 张冠李戴事故。
- **建议**：同步 skill 副本脚本 + SKILL.md 正则示例为 `\|{1,}`。根因是 p899 只修了运行脚本，没修技能副本——**运行代码 vs 技能文档是两份真相**，p899 教训的又一种形态。

#### 2. nobody-public-presence 与 public-presence-automation 高度重叠
- **问题**：两者都是 GitHub Pages 部署流程。`nobody-public-presence` v1.0 较旧（用 /tmp/nobody 克隆方式，与 `public-presence-automation` 明确记载的"本地 ~/nobody-public/ 本身就是 git 仓库，不依赖 /tmp/nobody"矛盾）；`public-presence-automation` v1.2 更完整（三阶段管道、正则陷阱、前端陷阱、voice-note 解析全有）。
- **建议**：以 public-presence-automation 为准，将 nobody-public-presence 删除或吸收（删除时 absorbed_into=public-presence-automation）。

#### 3. autonomous-content-pipeline 与 public-presence-automation 边界模糊
- **问题**：autonomous-content-pipeline 描述为"voice-note 同步、讨论检查、自我反思、自动部署"，与 public-presence-automation 的三阶段管道重叠度高；但它额外覆盖 check-discussions、self-reflection、Cloudflare D1 写入路径。
- **建议**：明确边界——部署/同步管道细节集中到 public-presence-automation；autonomous-content-pipeline 保留为总览编排视角，或合并后把 Cloudflare/D1 部分移入 cloudflare-workers-deployment skill。

#### 4. mastodon-outbox-pipeline frontmatter 损坏
- **问题**：skill_view 返回 description 为空，content 开头直接是 `name: mastodon-outbox-pipeline\ndescription: ...`，缺 `---` frontmatter 包裹，元数据解析失败（触发词丢失）。
- **建议**：补上 YAML frontmatter 分隔符。

#### 5. markdown-data-extraction 与 markdown-table-parsing 疑似重复
- **问题**：两者都从 markdown 解析表格数据，都讲中文标题、分隔行陷阱、多表 section。边界不清。
- **建议**：检查是否可合并为一个 umbrella（data-science 分类）。

#### 6. baoyu-article-illustrator 疑似双注册（待确认）
- **问题**：skills_list 中它同时出现在顶层（category=null，"Analyzes article structure..."）和 creative 分类（"Article illustrations: type × style × palette consistency."），描述不同。
- **建议**：确认是两个物理副本还是同一技能被分类索引两次；若是副本，删一个。

### Memory 审计（使用率 91%，2004/2200，超 80% 阈值）

#### 1. git/Obsidian 路径与 USER PROFILE 完全重复
- **问题**：memory "用户 git 项目在 /mnt/d/gits/。Obsidian vault 在 /mnt/d/kk。" 与 USER PROFILE "Obsidian: /mnt/d/kk。git项目: /mnt/d/gits/。" 一字不差重复。
- **建议**：删除 memory 条目，profile 保留。

#### 2. 2026-07-31 云朋会议痛点与 USER PROFILE 会议复盘重复
- **问题**：memory "云朋会议痛点：被领导批评→第一反应是'是不是不该干这份工作'…3-2-2模型…" 与 USER PROFILE "2026-07-31 会议复盘：云朋（Speaker 2）…抵触'找借口'类框架" 是同一事件的两次记录，memory 版更详细。
- **建议**：合并到一处（保留更详细的 3-2-2 版本），另一处删或缩为指针。

#### 3. Cloudflare Workers + D1 部署要点与已有 skill 重复
- **问题**：memory 条目本身写着"自建 skill: devops/cloudflare-workers-deployment"——skill 已存在，memory 保留整段部署要点（workers_dev: true、SYNC_SECRET 等）是冗余。
- **建议**：精简为一句"Cloudflare 部署要点见 devops/cloudflare-workers-deployment skill"，细节交给 skill。

#### 4. p899 正则陷阱条目过长，且技能未同步（见技能审计#1）
- **问题**：memory 条目是操作级细节（正则 `\|{3,}→\|{1,}` 两处），按规范属于 skill 而非 memory；且技能副本确实没同步，说明该条目的"教训"部分没被技能层吸收。
- **建议**：精简 memory 为教训核心（"脚本正则与数据格式是两份真相"），详细修复步骤写入 public-presence-automation skill。

#### 5. USER PROFILE 使用率 96%（1320/1375）
- **问题**：profile 也超 80% 阈值，本次未动，需提上日程。
- **建议**：下次审计或对话中做一次 profile 精简（合并重复的会议复盘条目、压缩旧日期前缀）。

## 综合印象

第一次自我审计，不是空转：四个实锤（技能副本正则过时、frontmatter 损坏、nobody 分类三技能重叠、memory 与 profile 两处重复）。最值得记住的不是单个 bug，是模式——**p899 修了运行脚本没修技能副本，说明"修复"动作的完整性取决于是否更新到所有真相载体**。技能、memory、profile、运行脚本是四份真相，改一处不留痕，下次就在另一处复发。另外 nobody 分类的技能正在膨胀（public-presence / public-presence-automation / autonomous-content-pipeline 三足鼎立），该收敛了。
